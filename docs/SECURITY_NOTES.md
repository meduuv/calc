# Security Notes

Calc runs entirely in the browser and does not require a backend for calculation. Keep future changes mindful of input handling and avoid introducing unsafe dynamic HTML or unnecessary third-party dependencies.
