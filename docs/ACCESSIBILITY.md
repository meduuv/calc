# Accessibility Notes

Calc should remain usable without relying only on pointer input.

- Keep all calculator controls keyboard reachable.
- Preserve visible focus states.
- Use descriptive labels for icon-only controls.
- Keep text and controls readable at common browser zoom levels.
- Avoid interactions that depend on color alone.
- Recheck keyboard operation after UI changes.
