# Architecture

Calc is a client-side browser application.

```text
User input
   ↓
Input handling
   ↓
Expression state
   ↓
Calculation
   ↓
Display + history
```

There is no server-side calculation layer.
